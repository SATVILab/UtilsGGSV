% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_merge_bin.R
\name{cluster_merge_bin}
\alias{cluster_merge_bin}
\title{Merge clusters based on variable bin thresholds}
\usage{
cluster_merge_bin(data, cluster, thresholds)
}
\arguments{
\item{data}{matrix or data.frame. Rows are observations, columns are
variables. Must contain columns whose names match those in
\code{thresholds}.}

\item{cluster}{character vector. Cluster assignment for each
observation. Length must equal \code{nrow(data)}.}

\item{thresholds}{named list. Each element is a numeric vector of
threshold values for the variable given by the element name. The
thresholds define bin boundaries: a single threshold \code{t} creates
two bins (\verb{<= t} and \verb{> t}), two thresholds create three bins, and
so on.}
}
\value{
A named list with two elements and a \code{"thresholds"} attribute:
\itemize{
\item \code{assign}: a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per observation and
columns \code{orig} (original cluster label) and \code{merged} (merged
cluster label).
\item \code{label}: a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per unique original
cluster and columns \code{orig} (original cluster label), \code{level}
(concise bin-combination label formed by joining per-variable bin
indices with \code{"_"}) and \code{descriptive} (human-readable
bin-combination description formed by joining per-variable range
descriptions with \code{"; "}).
}

The \code{"thresholds"} attribute stores the input \code{thresholds} list so
that downstream functions such as \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}} can
re-use the bin boundaries without requiring the caller to pass them
again.
}
\description{
Assigns each cluster to a bin for each variable based on where the
cluster's median falls relative to the provided thresholds. Clusters
that fall in the same combination of bins across all variables are
merged.
}
\examples{
set.seed(1)
mat <- matrix(
  c(rnorm(20, 2), rnorm(20, -2)),
  ncol = 2,
  dimnames = list(NULL, c("var1", "var2"))
)
cl <- rep(c("A", "B", "C", "D"), each = 5)
thresholds <- list(var1 = 0, var2 = 0)
cluster_merge_bin(mat, cl, thresholds)
}
