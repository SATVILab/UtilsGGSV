% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_som.R
\name{cluster_som}
\alias{cluster_som}
\title{Self-Organizing Map (SOM) clustering}
\usage{
cluster_som(
  data,
  vars = NULL,
  x_dim = 10L,
  y_dim = 10L,
  rlen = 10L,
  topo = "rectangular"
)
}
\arguments{
\item{data}{matrix or data.frame.
Rows are observations, columns are variables.}

\item{vars}{character vector or \code{NULL}.
Column names to use for clustering.
If \code{NULL} (default), all columns are used.}

\item{x_dim}{integer(1).
Number of columns in the SOM grid. Default \code{10L} (matching
FlowSOM).}

\item{y_dim}{integer(1).
Number of rows in the SOM grid. Default \code{10L} (matching
FlowSOM).}

\item{rlen}{integer(1).
Number of training iterations. Default \code{10L} (matching
FlowSOM).}

\item{topo}{character(1).
Grid topology passed to \code{\link[kohonen:unit.distances]{kohonen::somgrid()}}.
One of \code{"rectangular"} (default) or \code{"hexagonal"}.}
}
\value{
A character vector of cluster labels, one per row of \code{data}.
Labels are of the form \code{"1"}, \code{"2"}, etc., corresponding to
SOM grid nodes.
}
\description{
Trains a Self-Organizing Map on the input data using
\code{\link[kohonen:supersom]{kohonen::som()}} and returns the mapping-unit (node) assignment
for every observation as a cluster label.
}
\details{
The function:
\enumerate{
\item Scales the selected columns to zero mean and unit variance.
\item Creates a SOM grid of size \code{x_dim} × \code{y_dim}.
\item Trains a SOM on the scaled data.
\item Returns the unit assignment for each observation.
}

The default grid size (10 × 10) and training length (\code{rlen = 10})
match the defaults used by FlowSOM's \code{BuildSOM}.

The \code{kohonen} package must be installed.
}
\examples{
set.seed(1)
mat <- matrix(
  c(rnorm(50, -3), rnorm(50, 3), rnorm(100, 0)),
  ncol = 2,
  dimnames = list(NULL, c("v1", "v2"))
)
cl <- cluster_som(mat, x_dim = 3, y_dim = 3)
table(cl)
}
