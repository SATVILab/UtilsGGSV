% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_sim.R
\name{cluster_sim}
\alias{cluster_sim}
\title{Simulate High-Dimensional Single-Cell Clusters (FAUST Method)}
\usage{
cluster_sim(
  n_samples = 20L,
  n_clusters = 125L,
  n_dims = 10L,
  n_cells_per_sample = 25000L,
  base_cluster_weights = NULL,
  expr_modes = c(0, 8),
  var_bounds = c(1, 2),
  mean_perturb_sd = 1/sqrt(2),
  transform = c("none", "faust_gamma"),
  spike_clusters = NULL,
  spike_samples = NULL,
  spike_fold_change = 2,
  noise_dims = NULL,
  distribution = c("normal", "t"),
  df = 3,
  batch_effect_shift = NULL,
  batch_samples = NULL,
  knockout_clusters = NULL,
  knockout_samples = NULL
)
}
\arguments{
\item{n_samples}{Integer. Number of total samples to simulate.}

\item{n_clusters}{Integer. Number of mixture components.}

\item{n_dims}{Integer. Number of markers/dimensions.}

\item{n_cells_per_sample}{Integer. Total observations per sample.}

\item{base_cluster_weights}{Numeric vector. Custom base weights. If \code{NULL},
uses the FAUST reference vector with uniform residual distribution.}

\item{expr_modes}{Numeric vector. The values used to construct base mean
vectors.}

\item{var_bounds}{Numeric vector of length 2. Min and max constraints for
variance in covariance matrices.}

\item{mean_perturb_sd}{Numeric. Standard deviation for the Gaussian used to
perturb means per sample.}

\item{transform}{Character. Options: \code{"none"} (MVN) or \code{"faust_gamma"} for
\verb{Gamma(1 + |x/4|)}.}

\item{spike_clusters}{Integer vector. Indices of the clusters to spike in
abundance.}

\item{spike_samples}{Integer vector. Indices of the samples to apply the
spike to (e.g., "responders").}

\item{spike_fold_change}{Numeric. The multiplier for the spiked cluster
weights.}

\item{noise_dims}{Integer. Number of nuisance noise columns to append,
sampled from a standard normal distribution. If \code{NULL} (default), no noise
columns are added.}

\item{distribution}{Character. Distribution used to generate cell data.
\code{"normal"} (default) uses \code{\link[MASS:mvrnorm]{MASS::mvrnorm()}}; \code{"t"} uses
\code{\link[mvtnorm:Mvt]{mvtnorm::rmvt()}} for heavier tails. Requires the \pkg{mvtnorm} package
when set to \code{"t"}.}

\item{df}{Numeric. Degrees of freedom for the multivariate t-distribution.
Only used when \code{distribution = "t"}. Default is \code{3}.}

\item{batch_effect_shift}{Numeric scalar or vector of length \code{n_dims}. Mean
shift applied to samples indicated by \code{batch_samples} to simulate a
batch effect. If \code{NULL} (default), no batch effect is applied.}

\item{batch_samples}{Integer vector. Indices of samples that receive the
batch effect shift. Must be provided together with \code{batch_effect_shift}.}

\item{knockout_clusters}{Integer vector. Indices of clusters whose
probability is set to zero in the samples given by \code{knockout_samples}.
Must be provided together with \code{knockout_samples}.}

\item{knockout_samples}{Integer vector. Indices of samples where
\code{knockout_clusters} are removed. Must be provided together with
\code{knockout_clusters}.}
}
\value{
A list containing:

\item{data}{A tibble of the simulated data with \code{sample_id}, \code{cell_id},
\code{cluster_id}, and one column per dimension (\code{dim_1}, \code{dim_2}, ...).
When \code{noise_dims} is set, additional columns \code{noise_1}, \code{noise_2}, ...
are appended.}
\item{metadata}{A list containing true parameters: base means, perturbed
means per sample, covariance matrices, and exact weight vectors.}
}
\description{
Generates synthetic high-dimensional single-cell data replicating the
simulation studies from the FAUST paper (Greene et al., 2021, \emph{Patterns}).
Supports configurable numbers of dimensions, samples, and clusters, along
with differential abundance spike-ins for benchmarking.
}
\examples{
set.seed(42)
sim <- cluster_sim(
  n_samples = 2, n_clusters = 3, n_dims = 4,
  n_cells_per_sample = 100
)
head(sim$data)
}
