% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_scatter.R
\name{plot_cluster_scatter}
\alias{plot_cluster_scatter}
\title{Biaxial scatter plot with cluster medians overlaid}
\usage{
plot_cluster_scatter(
  .data,
  cluster,
  dim_red = NULL,
  vars = NULL,
  point_col_var = NULL,
  point_col = NULL,
  point_size = 2,
  point_alpha = 0.65,
  centroid_size = 3,
  label_size = 4,
  label_offset = 0.3,
  ggrepel = TRUE,
  font_size = 14,
  x_lab = NULL,
  y_lab = NULL,
  show_legend = TRUE,
  thm = cowplot::theme_cowplot(font_size = font_size) + ggplot2::theme(plot.background =
    ggplot2::element_rect(fill = "white", colour = NA), panel.background =
    ggplot2::element_rect(fill = "white", colour = NA)),
  grid = cowplot::background_grid(major = "xy")
)
}
\arguments{
\item{.data}{data.frame. Rows are observations. Must contain a column identifying cluster membership and numeric variables.}

\item{cluster}{character. Name of the column in \code{.data} that identifies cluster membership.}

\item{dim_red}{character or \code{NULL}. Dimensionality reduction method: one of \code{"none"}, \code{"pca"}, \code{"tsne"}, \code{"umap"}. If \code{NULL}, auto-selects \code{"none"} when exactly 2 numeric vars are available, otherwise \code{"pca"}.}

\item{vars}{character vector or \code{NULL}. Names of numeric columns in \code{.data} to use for the plot or reduction. If \code{NULL}, uses all numeric columns except \code{cluster} and \code{point_col_var}.}

\item{point_col_var}{character or \code{NULL}. Column to use for point colour mapping. Default is same as \code{cluster}.}

\item{point_col}{named vector or NULL. Custom colours for discrete \code{point_col_var} (named by level) or colour bounds for continuous \code{point_col_var} (length 3 low/mid/high).}

\item{point_size}{numeric. Size of observation points. Default is \code{2}.}

\item{point_alpha}{numeric. Alpha transparency for observation points and legend guide. Default is \code{0.65}.}

\item{centroid_size}{numeric. Size of centroid points. Default is \code{3}.}

\item{label_size}{numeric. Font size for centroid labels. Default is \code{4}.}

\item{label_offset}{numeric. Label repulsion padding for centroid labels in cm. Default is \code{0.3}.}

\item{ggrepel}{logical. Use \code{ggrepel::geom_text_repel} for centroid labels. Default is \code{TRUE}.}

\item{font_size}{numeric. Font size passed to \code{cowplot::theme_cowplot}. Default is \code{14}.}

\item{x_lab}{character or \code{NULL}. Label for x axis; default uses reduction variable names.}

\item{y_lab}{character or \code{NULL}. Label for y axis; default uses reduction variable names.}

\item{show_legend}{logical. Whether to show the legend. Default is \code{TRUE}.
Set to \code{FALSE} to hide the legend, as centroid labels may suffice.}

\item{thm}{ggplot2 theme object or \code{NULL}. Default is \code{cowplot::theme_cowplot(font_size = font_size)} with white background.}

\item{grid}{ggplot2 layer or \code{NULL}. Background grid added to the plot.
Default is \code{cowplot::background_grid(major = "xy")}. Set to \code{NULL} to
suppress the grid.}
}
\description{
Creates a biaxial scatter plot with observations colored by cluster assignment.
Median cluster centroids are overlaid as large points and labeled with cluster names.
The plot can use either raw variables (specified by the user) or dimensionality
reduction components as axes.
}
\examples{
set.seed(1)
data <- data.frame(
  cluster = rep(paste0("C", 1:3), each = 20),
  var1 = c(rnorm(20, 2), rnorm(20, 0), rnorm(20, -2)),
  var2 = c(rnorm(20, -1), rnorm(20, 1), rnorm(20, 0)),
  var3 = c(rnorm(20, 1), rnorm(20, -1), rnorm(20, 0))
)
plot_cluster_scatter(data, cluster = "cluster")
plot_cluster_scatter(data, cluster = "cluster", dim_red = "none", vars = c("var1", "var2"))
plot_cluster_scatter(data, cluster = "cluster", show_legend = FALSE)
}
