% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_scatter.R
\name{plot_group_scatter}
\alias{plot_group_scatter}
\alias{plot_cluster_scatter}
\title{Biaxial scatter plot with group medians overlaid}
\usage{
plot_group_scatter(
  .data,
  group,
  dim_red = NULL,
  vars = NULL,
  dim_red_args = list(),
  point_col_var = NULL,
  point_col = NULL,
  palette = "bipolar",
  col = c("#2166AC", "#F7F7F7", "#B2182B"),
  col_positions = "auto",
  white_range = c(0.4, 0.6),
  na_rm = TRUE,
  point_size = 2,
  point_alpha = 0.65,
  centroid_size = 3,
  label_size = 4,
  label_offset = 0.3,
  ggrepel = TRUE,
  font_size = 14,
  x_lab = NULL,
  y_lab = NULL,
  show_legend = TRUE,
  thm = cowplot::theme_cowplot(font_size = font_size) + ggplot2::theme(plot.background =
    ggplot2::element_rect(fill = "white", colour = NA), panel.background =
    ggplot2::element_rect(fill = "white", colour = NA)),
  grid = cowplot::background_grid(major = "xy")
)

plot_cluster_scatter(.data, cluster, ...)
}
\arguments{
\item{.data}{data.frame. Rows are observations. Must contain a column identifying group membership and numeric variables.}

\item{group}{character. Name of the column in \code{.data} that identifies group membership.}

\item{dim_red}{character or \code{NULL}. Dimensionality reduction method: one of \code{"none"}, \code{"pca"}, \code{"tsne"}, \code{"umap"}. If \code{NULL}, auto-selects \code{"none"} when exactly 2 numeric vars are available, otherwise \code{"pca"}.}

\item{vars}{character vector or \code{NULL}. Names of numeric columns in \code{.data} to use for the plot or reduction. If \code{NULL}, uses all numeric columns except \code{cluster} and \code{point_col_var}.}

\item{dim_red_args}{named list. Additional arguments passed to the
dimensionality reduction function, overriding any defaults set by
\code{plot_cluster_scatter}. For \code{dim_red = "pca"} these are passed to
\code{\link[stats:prcomp]{stats::prcomp()}} (default: \code{scale. = TRUE}); for \code{dim_red = "tsne"} to
\code{\link[Rtsne:Rtsne]{Rtsne::Rtsne()}} (defaults: \code{dims = 2}, \code{perplexity} auto-computed,
\code{check_duplicates = FALSE}, \code{pca = FALSE}); for \code{dim_red = "umap"} to
\code{\link[umap:umap]{umap::umap()}}. The data argument is always set internally and cannot be
overridden. Ignored when \code{dim_red = "none"}. Default is \code{list()}.}

\item{point_col_var}{character or \code{NULL}. Column to use for point colour mapping. Default is same as \code{cluster}.}

\item{point_col}{named vector or \code{NULL}. Custom colours for discrete \code{point_col_var} (named by
level). Ignored for continuous \code{point_col_var} (use \code{col} instead).}

\item{palette}{character or \code{NULL}. Named colour palette for the continuous \code{point_col_var} colour
scale. When not \code{NULL}, overrides \code{col} and \code{col_positions}. Available palettes:
\code{"bipolar"} (default, blue-white-red), \code{"alarm"} (green-white-red, good-to-bad),
\code{"accessible"} (blue-white-orange, colour-blind-safe diverging), \code{"heat"} (light-yellow to
dark-red, sequential), \code{"sky"} (white to navy, sequential). Set to \code{NULL} to use \code{col} and
\code{col_positions} directly. Ignored when \code{point_col_var} is discrete.}

\item{col}{character vector. Colours used for the continuous \code{point_col_var} colour scale, ordered
from low to high values. Default is \code{c("#2166AC", "#F7F7F7", "#B2182B")} (blue, white, red).
Any number of colours (>= 2) is accepted. Ignored when \code{point_col_var} is discrete or when
\code{palette} is not \code{NULL}.}

\item{col_positions}{numeric vector or \code{"auto"}. Positions (in [0, 1]) at which each colour in
\code{col} is placed on the colour scale. Must be the same length as \code{col}, sorted ascending, with
first value \code{0} and last value \code{1}. When \code{"auto"} (default) and \code{col} has exactly three colours,
the middle colour is stretched over \code{white_range}. In all other \code{"auto"} cases the colours are
evenly spaced from 0 to 1. Ignored when \code{point_col_var} is discrete or when \code{palette} is not
\code{NULL}.}

\item{white_range}{numeric vector of length 2. The range of positions (on a 0-1 scale) over which
the middle colour is stretched. Only used when \code{col} has exactly three colours and
\code{col_positions = "auto"}. Also applied to diverging \code{palette} presets. Default is \code{c(0.4, 0.6)}.
Ignored when \code{point_col_var} is discrete.}

\item{na_rm}{logical. Whether to remove observations with missing values in the variables used
for the plot axes (and dimensionality reduction, when applicable). When \code{TRUE} (default),
missing observations are removed and a message is issued showing how many. When \code{FALSE} and
\code{dim_red} is not \code{"none"}, an error is raised if any missing values are found (dimensionality
reduction algorithms cannot handle them). When \code{FALSE} and \code{dim_red = "none"}, observations
with missing axis values are silently dropped by ggplot2.}

\item{point_size}{numeric. Size of observation points. Default is \code{2}.}

\item{point_alpha}{numeric. Alpha transparency for observation points and legend guide. Default is \code{0.65}.}

\item{centroid_size}{numeric. Size of centroid points. Default is \code{3}.}

\item{label_size}{numeric. Font size for centroid labels. Default is \code{4}.}

\item{label_offset}{numeric. Label repulsion padding for centroid labels in cm. Default is \code{0.3}.}

\item{ggrepel}{logical. Use \code{ggrepel::geom_text_repel} for centroid labels. Default is \code{TRUE}.}

\item{font_size}{numeric. Font size passed to \code{cowplot::theme_cowplot}. Default is \code{14}.}

\item{x_lab}{character or \code{NULL}. Label for x axis; default uses reduction variable names.}

\item{y_lab}{character or \code{NULL}. Label for y axis; default uses reduction variable names.}

\item{show_legend}{logical. Whether to show the legend. Default is \code{TRUE}.
Set to \code{FALSE} to hide the legend, as centroid labels may suffice.}

\item{thm}{ggplot2 theme object or \code{NULL}. Default is \code{cowplot::theme_cowplot(font_size = font_size)} with white background.}

\item{grid}{ggplot2 layer or \code{NULL}. Background grid added to the plot.
Default is \code{cowplot::background_grid(major = "xy")}. Set to \code{NULL} to
suppress the grid.}

\item{cluster}{character. Name of the column in \code{.data} that identifies
group membership. Alias for the \code{group} parameter.}
}
\description{
Creates a biaxial scatter plot with observations colored by group assignment.
Median group centroids are overlaid as large points and labeled with group names.
The plot can use either raw variables (specified by the user) or dimensionality
reduction components as axes.
}
\examples{
set.seed(1)
.data <- data.frame(
  group = rep(paste0("C", 1:3), each = 20),
  var1 = c(rnorm(20, 2), rnorm(20, 0), rnorm(20, -2)),
  var2 = c(rnorm(20, -1), rnorm(20, 1), rnorm(20, 0)),
  var3 = c(rnorm(20, 1), rnorm(20, -1), rnorm(20, 0))
)
plot_group_scatter(.data, group = "group")
plot_group_scatter(.data, group = "group", dim_red = "none", vars = c("var1", "var2"))
plot_group_scatter(.data, group = "group", show_legend = FALSE)
# Pass extra arguments to the dim-red function, e.g. disable scaling in PCA:
plot_group_scatter(.data, group = "group", dim_red = "pca",
                     dim_red_args = list(scale. = FALSE))
}
