% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_louvain.R
\name{cluster_louvain}
\alias{cluster_louvain}
\title{Louvain community-detection clustering}
\usage{
cluster_louvain(data, vars = NULL, k = 15L, resolution = 1)
}
\arguments{
\item{data}{matrix or data.frame.
Rows are observations, columns are variables.}

\item{vars}{character vector or \code{NULL}.
Column names to use for clustering.
If \code{NULL} (default), all columns are used.}

\item{k}{integer(1).
Number of nearest neighbours used to build the kNN graph.
Default \code{15L}.}

\item{resolution}{numeric(1).
Resolution parameter passed to \code{\link[igraph:cluster_louvain]{igraph::cluster_louvain()}}.
Higher values yield more (smaller) clusters.
Default \code{1}.}
}
\value{
A character vector of cluster labels, one per row of \code{data}.
Labels are of the form \code{"1"}, \code{"2"}, etc.
}
\description{
Builds a shared nearest-neighbour graph from the input data and applies
the Louvain algorithm (\code{\link[igraph:cluster_louvain]{igraph::cluster_louvain()}}) to detect
communities.
}
\details{
The function:
\enumerate{
\item Scales the selected columns to zero mean and unit variance.
\item Finds the \code{k} nearest neighbours for each observation using
Euclidean distance (via \code{\link[FNN:get.knn]{FNN::get.knn()}}).
\item Constructs a shared-nearest-neighbour (SNN) graph where the
edge weight between two observations equals the number of
shared neighbours.
\item Applies Louvain community detection on the SNN graph.
}
}
\examples{
set.seed(1)
mat <- matrix(
  c(rnorm(50, -3), rnorm(50, 3), rnorm(100, 0)),
  ncol = 2,
  dimnames = list(NULL, c("v1", "v2"))
)
cl <- cluster_louvain(mat, k = 10)
table(cl)
}
