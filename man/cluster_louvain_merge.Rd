% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_louvain.R
\name{cluster_louvain_merge}
\alias{cluster_louvain_merge}
\title{Louvain clustering with bin and unimodal merging}
\usage{
cluster_louvain_merge(
  data,
  vars = NULL,
  thresholds,
  k = 15L,
  resolution = 1,
  max_label_diff = 2L,
  ignore_labels = NULL,
  dip_threshold = 0.15,
  min_mode_dist = NULL,
  max_iterations = 50L
)
}
\arguments{
\item{data}{matrix or data.frame.
Rows are observations, columns are variables.}

\item{vars}{character vector or \code{NULL}.
Column names to use for clustering.
If \code{NULL} (default), all columns are used.}

\item{thresholds}{named list.
Passed to \code{\link[=cluster_merge_bin]{cluster_merge_bin()}}. Each element is a numeric vector
of threshold values for the variable given by the element name.}

\item{k}{integer(1).
Number of nearest neighbours used to build the kNN graph.
Default \code{15L}.}

\item{resolution}{numeric(1).
Resolution parameter passed to \code{\link[igraph:cluster_louvain]{igraph::cluster_louvain()}}.
Higher values yield more (smaller) clusters.
Default \code{1}.}

\item{max_label_diff}{integer(1).
Passed to \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}. Default \code{2L}.}

\item{ignore_labels}{character vector or \code{NULL}.
Passed to \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}. Default \code{NULL}.}

\item{dip_threshold}{numeric(1).
Passed to \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}. Default \code{0.15}.}

\item{min_mode_dist}{numeric or named numeric vector or \code{NULL}.
Passed to \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}. Default \code{NULL}.}

\item{max_iterations}{integer(1).
Passed to \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}. Default \code{50L}.}
}
\value{
The list returned by \code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}, with
elements \code{assign} and \code{label} and a \code{"thresholds"} attribute.
}
\description{
End-to-end wrapper that applies Louvain clustering
(\code{\link[=cluster_louvain]{cluster_louvain()}}), then merges clusters by variable bin
thresholds (\code{\link[=cluster_merge_bin]{cluster_merge_bin()}}), and finally merges unimodal
neighbours (\code{\link[=cluster_merge_unimodal]{cluster_merge_unimodal()}}).
}
\examples{
set.seed(1)
mat <- matrix(
  c(rnorm(50, -3), rnorm(50, 3), rnorm(100, 0)),
  ncol = 2,
  dimnames = list(NULL, c("v1", "v2"))
)
result <- cluster_louvain_merge(
  mat,
  vars = c("v1", "v2"),
  thresholds = list(v1 = 0, v2 = 0),
  k = 10
)
table(result$assign$merged)
}
